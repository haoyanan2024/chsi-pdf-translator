# 参与改进

欢迎通过 Issues 报告问题、建议词库条目或讨论新版式，再提交 Pull Request。

## 报告问题

说明应用版本、操作系统、输入是否为学信网原始学籍 PDF、复现步骤、预期结果和实际结果。截图须使用虚构数据或彻底脱敏的数据。

请勿在公开 Issue、PR、提交记录或测试文件中放入真实报告、照片、二维码、验证码、学号、草稿、服务器地址或密码。仅在 PDF 上覆盖白色矩形并不一定清除底层文本与图片。复现样本优先使用 `chsi_translator/selftest.py` 中的合成文档生成方式。

## 提交代码

1. Fork 仓库并创建功能分支。
2. 按 [开发指南](docs/DEVELOPMENT.md) 创建虚拟环境。
3. 对提取、翻译或排版变更补充有意义的合成样例回归测试。
4. 执行 `python -m pytest -q` 和 `python -m chsi_translator --self-test --headless`；GUI 变更再运行图形自检并人工检查。
5. 涉及 PDF 布局时，把输出渲染成图片检查花边、主表、照片与核验区，不只比较提取出的文本。
6. 更新对应文档和 CHANGELOG，提交简明的 PR 说明与验证结果。

新增词库条目时请附学校、机构或其他可靠来源的正式英文译名链接，不用机器翻译结果冒充官方名称。程序应继续允许人工校准。

## 许可证与资源

贡献代码适用项目的 Apache-2.0 许可证。保留 LICENSE、NOTICE、THIRD_PARTY_NOTICES.md 以及第三方许可证。新增模板、字体或依赖时说明来源、版本和许可，不提交来源不明的资源。构建和发布流程见 [发布指南](docs/PUBLISHING.md)。
